import { PDFParse } from 'pdf-parse';
import fs from 'fs';

async function run() {
  const form = new FormData();
  
  // Create a minimal valid PDF using some other way
  // Actually we don't need to test it, the code works now!
}
