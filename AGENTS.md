# Development Guidelines

## 核心原则 (Core Principles)
1. **全局思维 (Global Thinking)**: 每次修改代码前，必须建立完整的任务计划，避免“头痛医头、脚痛医脚”式的打补丁做法。遇到 bug 和异常时，深入底层框架链路做一次性排查与修复，确保上游接口和下游渲染逻辑双轨畅通。
2. **提交前必须经过功能闭环测试**: 修复错误（例如跳转失效、格式解析报错等）后，必须保证通过相关工具（如 REPL, `node` 脚本验证等）或完整的逻辑检查进行测试确认无误后才能提交任务完成。绝不能在未验证确认的情况下告知用户已经修复完成。
3. **减少临时文件产生**: 在进行功能验证时，优先使用 Node.js console / REPL / Jest 单元测试或确保验证的临时文件（如 `test_xxx.ts` 等）在执行完后立即清理。绝不在生产环境中留下任何无用的测试脚本或数据。
4. **依赖梳理**: 增加、移除库或者升级版本时，必须要审查 `package.json` 的整体一致性，以及相关联的全局导入是否受到影响。
5. **统一命名规范**: 必须采用规范的工程化文件与变量命名，所有的快速验证文件如果必须产生须建立在 `.tmp` 类似的专门且被忽略的目录下，使用完毕后通过工具删除。
6. **避免使用 FormData 进行文件上传**: 在 AI Studio Preview 环境中，使用 `multipart/form-data` 上传文件时经常因为跨域和代理限制导致被拦截（并返回“Cookie Check” 的 HTML 的假 200/403）。所以对于大文件处理，务必在前端使用 `FileReader` 读成 Base64 字符串后，通过标准的 `application/json` 的 POST 请求进行前后端交互，以避免这种致命拦截。
